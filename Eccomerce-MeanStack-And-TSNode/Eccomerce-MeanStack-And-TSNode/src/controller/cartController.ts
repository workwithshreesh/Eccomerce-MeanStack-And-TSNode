import {Request, Response} from 'express';
import { AppDataSource } from '../config/data-source';



async function createCart(req:Request, res:Response){
    try{

        

    } catch(error){
        console.log("Error in cart create", error);
        throw error;
    }
}